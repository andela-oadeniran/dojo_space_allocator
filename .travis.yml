language: python

python:
  - "3.5"

# command to install dependencies
install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"

# command to run tests
script:
  - "nosetests
    --ignore-files='test_dojo_load_people.py'
    --with-coverage
    tests"
  - "coveralls"
