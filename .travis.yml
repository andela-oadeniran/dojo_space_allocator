language: python

python:
  - "3.5"

# command to install dependencies
install:
  - "pip install -r requirements.txt"
  
# command to run tests
script:
  - "nosetests
    --ignore-files='test_dojo_reallocate_load_people\.py' 
    tests"
after-success:
  - "coveralls"
